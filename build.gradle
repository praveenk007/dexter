plugins {
    id 'com.github.blindpirate.gogradle' version '0.11.2'
}

golang {
    packagePath = 'src/dexter'
}


dependencies {
    golang {
        build(['name':'github.com/gorilla/mux'])
        build(['name':'github.com/mongodb/mongo-go-driver/mongo'])
        
    }
}